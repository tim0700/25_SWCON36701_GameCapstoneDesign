# Memory System Configuration
RECENT_MEMORY_SIZE=5
LONG_TERM_BUFFER_SIZE=10
SIMILARITY_SEARCH_RESULTS=3

# Embedding Configuration
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Device options for embedding model:
# - auto: Automatically detect GPU (CUDA on Windows/Linux, MPS on Mac)
# - cpu: Force CPU (slower but works on all platforms)
# - cuda: Force NVIDIA GPU (requires CUDA toolkit on Windows/Linux)
# - mps: Force Apple Silicon GPU (Mac only, not supported on Windows)
EMBEDDING_DEVICE=auto

PRELOAD_ON_STARTUP=true
MAX_BATCH_SIZE=50

# Storage Configuration
CHROMA_PERSIST_DIR=./data/chroma_db
BUFFER_DIR=./data/buffers
RECENT_MEMORY_BACKUP=./data/recent_memory.json

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_TITLE=NPC Dynamic Memory System
API_VERSION=1.0.0

# Logging
LOG_LEVEL=INFO
